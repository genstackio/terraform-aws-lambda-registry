# AWS regional-registry Terraform module

Terraform module which create a regional-registry on AWS.

## Usage

```hcl
module "regional-registry" {
  source = "genstackio/layer-lambda-registry/aws//modules/regional-registry"

  // ...
}
```
