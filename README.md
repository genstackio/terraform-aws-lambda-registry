# AWS Lambda Registry Layer Terraform module

Terraform module which creates a lambda registry layer on AWS.

## Usage

```hcl
module "main" {
  source     = "genstackio/layer-lambda-registry/aws"

  // ...
}
```
